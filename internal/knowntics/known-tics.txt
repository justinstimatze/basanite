# known-tics — a conservative *shipping sample* of words and phrases Claude is
# globally known to lean on, biased toward the ones that actually recur in
# Claude Code transcripts. Seeded from the community "Claude Bingo" card and
# the well-worn assistant-register staples. Kept high-precision on purpose:
# this is a sensible default everyone can ship with, not an exhaustive list.
#
# Format mirrors proper-nouns.txt: one entry per line, '#' comments, blank
# lines ignored. A line with a space is a phrase, matched verbatim against the
# surface word stream; anything else is a single word, lemmatized to match
# corpus tokens. Extend it with your own known-tics.txt in the data dir or
# ~/.config/basanite — later files add to this list, never replace it; that's
# where the niche or personal leans belong.
#
# These are a *reference*, not a denylist: a seeded entry still has to be
# something you're actually leaning on now (steady rate, dispersed) before it
# surfaces, and the output stays awareness, never prohibition.

# --- single words (distinctive Claude register; common-English leans like
#     surface/frame/honor are deliberately left for your local list) ---
load-bearing
substrate
calibration
liminal
texture

# --- assistant-register phrases (the globally common ones) ---
you're absolutely right
you're absolutely correct
great question
happy to help
to be clear
worth noting
that said
at the end of the day
doing a lot of work

# --- signature phrases (the iconic Claude ones, seeded from the card) ---
that's not nothing
the thing underneath the thing
i want to honor that
let's stay with that
i want to be careful here
